# environment file only, no executable code;used to test against env-security-check.sh

#quality testing
#the following tests blank lines

#the following line test blank lines with a mix of tabs and spaces
		
var="$(test)"
something=(ls)
echo "I'm bad!"
TEST_TWO="farts"
PATH="evil"
USER="mrclean"
first_arg="$1"
#valid entry
trest="three"
something2="`ls`"
